# Revisão — Prisma Inicial

📋 **Painel de Sinais Organizados (Compacto)**  
🔍[CHECK] 📚[STUDY] 💾[DB]

## Perguntas

1. Prisma é o banco de dados?
2. Qual é o papel do PostgreSQL?
3. Para que serve `schema.prisma`?
4. O que um `model` representa?
5. O que significa `String?`?
6. O que significa `@id`?
7. Para que serve `@unique`?
8. O que faz `@default(...)`?
9. Para que serve `prisma migrate dev`?
10. O que é `migration.sql`?
11. Qual é a diferença entre `migrate` e `generate`?
12. O que é seed?
